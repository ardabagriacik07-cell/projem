@model ServisFormViewModel
@{
    ViewData["Title"] = "Yeni Servis";
    ViewData["PageTitle"] = "Yeni Servis Kaydı";
    ViewData["Subtitle"] = "Cihaz seç, durum belirle, işlemleri ekle.";
}

<div class="card">
    <div class="card-body p-4 p-lg-5">
    <span class="admin-chip">Yeni İş Emri</span>
    <h3 class="mt-3">Yeni Servis Kaydı</h3>
    <p class="text-secondary mb-4">Cihazı seç, durum belirle ve yapılacak işlemleri tek formdan bağla.</p>

    <form asp-action="Create" method="post" class="form-grid">
        @Html.AntiForgeryToken()
        <div asp-validation-summary="ModelOnly" class="validation full"></div>

        <div class="field full">
            <label>Cihaz</label>
            <select asp-for="Servis.CihazId">
                <option value="">Cihaz seçiniz</option>
                @foreach (var c in Model.Cihazlar)
                {
                    <option value="@c.Id">@c.Musteri?.AdSoyad - @c.Marka @c.Model</option>
                }
            </select>
        </div>

        <div class="field">
            <label>Tarih</label>
            <input asp-for="Servis.Tarih" type="date" class="input" />
        </div>

        <div class="field">
            <label>Durum</label>
            <select asp-for="Servis.Durum">
                <option>Bekliyor</option>
                <option value="Islemde">İşlemde</option>
                <option value="Fiyat Onayi Bekliyor">Fiyat Onayı Bekliyor</option>
                <option value="Tamamlandi">Tamamlandı</option>
                <option>Teslim Edildi</option>
                <option>Fiyat Reddedildi</option>
            </select>
        </div>

        <div class="field">
            <label>Fiyat Onayı</label>
            <label class="checkbox-item">
                <span>Müşteri ekranına fiyat onayı düşür</span>
                <input asp-for="FiyatiOnayaGonder" />
            </label>
        </div>

        <div class="field full">
            <partial name="_IslemSelector" model="Model" />
        </div>

        <div class="link-row full">
            <button type="submit" class="btn btn-primary">Kaydet</button>
            <a asp-action="Index" class="btn btn-light">Vazgeç</a>
        </div>
    </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
